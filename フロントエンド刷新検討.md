# フロントエンド刷新 検討資料

## 現状の問題点

| 問題 | 詳細 |
|---|---|
| HTML がデカい | テンプレート合計 ~5,700行、うち JS ~2,200行・CSS ~1,700行がインライン埋め込み |
| 絵文字滥用 | ナビゲーション・ステータス表示に約25種類の絵文字を使用 |
| CSS が重複 | 全5テンプレートに同じようなダークテーマ用 CSS が各 350-550 行ずつインライン（合計 ~1,700行分散） |
| 外部CDN | logs/backups/api_docs で Google Fonts を CDN 参照（index.html はシステムフォントのみ） |
| 型の不在 | API レスポンスの型定義なし、JS が動的プロパティアクセスで構成 |

## アーキテクチャ比較

### 現状: サーバーサイドテンプレート + Vanilla JS

```
Browser ← HTML (Go template) + inline JS/CSS
         ↕ fetch() + JSON
         Go Backend (chi)
```

- **メリット**: ビルド不要、依存ゼロ、Go バイナリだけで完結
- **デメリット**: テンプレートが太る、JS がグローバルスコープ、CSS が重複
- **バンドルサイズ**: Go バイナリ 13.7MB のみ（fonts 含まず）

### 案 A: React (Vite + React + TypeScript)

```
Browser ← static files (Vite build)
         ↕ fetch() + JSON
         Go Backend (chi, API only)
         templates/ → 全削除（SPA 化）
```

**構成例**:
```
frontend/
├── package.json
├── vite.config.ts
├── tsconfig.json
├── src/
│   ├── main.tsx
│   ├── App.tsx             # Router
│   ├── components/
│   │   ├── SwitchCard.tsx  # スイッチカード
│   │   ├── PortTable.tsx   # ポート一覧
│   │   ├── SpeedGraph.tsx  # 帯域グラフ
│   │   ├── SFPDiag.tsx     # SFP DDMI
│   │   ├── MACTable.tsx    # MAC テーブル
│   │   └── TopologyMap.tsx # トポロジーマップ
│   ├── pages/
│   │   ├── Dashboard.tsx
│   │   ├── Logs.tsx
│   │   ├── Backups.tsx
│   │   └── Config.tsx
│   ├── hooks/
│   │   └── useAPI.ts       # fetch wrapper
│   └── types/
│       └── api.ts          # API 型定義
└── index.html
```

| 項目 | 評価 |
|---|---|
| 工数 | 3-5日（コンポーネント分割 + ルーティング） |
| バンドルサイズ | ~150KB gzip（React + ReactDOM + コード） |
| node_modules | ~80MB（Vite + React + TypeScript + プラグイン） |
| ビルド時間 | 初回 ~30秒、差分 ~1秒 |
| 開発体験 | HMR、TypeScript、ESLint |
| Go 側変更 | テンプレート廃止 → API のみに。`cmd/` の変更は最小限 |
| デプロイ | `frontend/dist/` を Go バイナリに embed するか、別途 nginx で配信 |

### 案 B: Bun + Hono フルスタック

```
Browser ← static files (Bun serve)
         ↕ fetch() + JSON
         Go Backend (chi, optional: or replace with Hono on Bun)
```

Bun をフロントエンドのビルドツール兼開発サーバーとして使う場合:

```
Bun runtime
├── frontend/
│   ├── src/
│   │   ├── index.html      # テンプレート（Bun.file で読むか、そのまま）
│   │   ├── app.ts          # TypeScript（Bun がネイティブ実行）
│   │   └── dashboard.ts    # 各ページのロジック
│   └── public/
│       └── style.css
└── bun.lock
```

| 項目 | 評価 |
|---|---|
| 工数 | 2-3日（JS/CSS の外出し + 型追加） |
| バンドルサイズ | ~50KB（Bun バンドラ出力、minify） |
| node_modules | なし（Bun は npm 互換、package.json は最小限） |
| ビルド時間 | ~5秒（Bun のバンドラは高速） |
| 開発体験 | Bun の `--watch` モード、TypeScript ネイティブ実行 |
| Go 側変更 | なし（バックエンドはそのまま） |
| デプロイ | `bun build` の出力を Go に embed、または Bun で配信 |

### 案 C: 現状の Vanilla JS をリファクタリング

```
Browser ← HTML + 外部化した JS/CSS
         ↕ fetch() + JSON
         Go Backend (chi)
```

**変更点**:
- `templates/` から JS を `static/*.js` に抽出（~2,200行）
- `templates/` から CSS を `static/*.css` に抽出（~1,700行）
- 絵文字を SVG または Unicode 記号に置換
- Google Fonts を CDN のまま維持 or セルフホスト
- index.html のインライン SVG グラフはそのまま

| 項目 | 評価 |
|---|---|
| 工数 | 0.5-1日（機械的抽出 + 絵文字置換） |
| バンドルサイズ | 変更なし |
| 依存 | なし |
| 開発体験 | 変わらず。ただし静的解析・lint は制限的 |
| Go 側変更 | `static/` のファイルを embed するよう変更 |

---

## 推奨: 案 B（Bun バンドラ）→ 将来的に案 A（React）

段階的アプローチ:

### Step 1: 現状のクリーンアップ（0.5日）← 何なら今すぐできる
- `templates/` から CSS を `static/style.css` に抽出（各テンプレートの 350-550 行を統合）
- `templates/` から JS を `static/app.js`（index.html）・`static/logs.js` などに分割
- 絵文字 → SVG アイコンに置き換え（`static/icons/` に SVGs）
- Go の `embed` で静的ファイルを配信（既に対応済み）

### Step 2: Bun 導入（1-2日）
- フロントエンドのビルドに Bun を使用
- JS に TypeScript の JSDoc 型定義を追加（API レスポンスの型）
- `bun build --target=browser` で `static/dist/` にバンドル
- Go が `static/dist/` を embed

### Step 3: React 移行（任意、3-5日）
- 本格的な SPA が必要になった場合のみ
- Vite + React + TypeScript で `frontend/` を独立
- `frontend/dist/` を Go に embed

## 比較表

| 観点 | 現状 | クリーンアップ (Step 1) | Bun (Step 2) | React (Step 3) |
|---|---|---|---|---|
| HTML 行数 | ~5,700行 | ~2,000行 | ~2,000行 | ~50行 (SPA shell) |
| CSS 重複 | 5箇所に分散 | 1ファイルに統合 | 1ファイル | CSS Module/ Tailwind |
| 型安全性 | なし | JSDoc コメント | TypeScript + JSDoc | TypeScript 完全 |
| ビルド依存 | なし | なし | Bun のみ | Node.js + 数十パッケージ |
| バンドルサイズ | インライン | ~50KB | ~50KB (minify) | ~150KB gzip |
| Go 依存 | テンプレート必須 | embed 静的ファイル | embed 静的ファイル | embed のみ |
| 工数 | - | 0.5日 | 2-3日 | +3-5日 |
| リスク | 低 | 極低 | 低 | 中（バンドル設定） |

**現実的な最短ルート**: Step 1 のクリーンアップを先に実施。その後に Step 2・3 を判断。
